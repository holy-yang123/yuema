.container {
  background-color: var(--bg);
  min-height: 100%;
  padding: 32rpx;
  box-sizing: border-box;
}

.room-info {
  background: var(--surface-solid);
  padding: 32rpx;
  border-radius: 16rpx;
  margin-bottom: 32rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border: 1rpx solid rgba(20, 20, 19, 0.08);
  box-shadow: 0 8rpx 24rpx rgba(20, 20, 19, 0.06);
}

.room-no {
  color: var(--text);
  font-size: 30rpx;
  font-weight: 600;
}

.game-type {
  color: var(--accent-orange);
  font-size: 26rpx;
  font-weight: 500;
}

.history-item {
  background: var(--surface-solid);
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  border: 1rpx solid rgba(20, 20, 19, 0.08);
  box-shadow: 0 8rpx 24rpx rgba(20, 20, 19, 0.05);
}

.round-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 24rpx;
  padding-bottom: 24rpx;
  border-bottom: 1rpx solid var(--bg-muted);
}

.round-no {
  color: var(--text);
  font-size: 28rpx;
  font-weight: 600;
}

.round-time {
  color: var(--text-secondary);
  font-size: 22rpx;
}

.player-scores {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24rpx;
}

.player-score {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nickname {
  color: var(--text-secondary);
  font-size: 26rpx;
}

.score {
  font-size: 28rpx;
  font-weight: 600;
}

.plus {
  color: var(--accent-green);
}

.minus {
  color: var(--danger);
}

.empty-state {
  display: flex;
  justify-content: center;
  padding-top: 160rpx;
  color: var(--text-secondary);
  font-size: 28rpx;
}
