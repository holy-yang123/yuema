.container {
  background-color: var(--bg);
  min-height: 100%;
  padding: 32rpx;
  padding-bottom: 180rpx;
  box-sizing: border-box;
}

.locked-tip {
  font-size: 24rpx;
  color: var(--text-secondary);
  margin-bottom: 20rpx;
  line-height: 1.5;
}

.modify-entry {
  margin-bottom: 24rpx;
}

.modify-modal-mask {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 200;
}

.modify-modal {
  position: fixed;
  left: 32rpx;
  right: 32rpx;
  top: 12vh;
  max-height: 75vh;
  overflow-y: auto;
  /* 与 app.wxss 页面级一致：弹层内滚动不显示滚动条 */
  scrollbar-width: none;
  -ms-overflow-style: none;
  background: var(--surface-solid);
  border: 1rpx solid rgba(20, 20, 19, 0.08);
  border-radius: 16rpx;
  padding: 28rpx;
  z-index: 210;
  box-shadow: 0 16rpx 48rpx rgba(20, 20, 19, 0.12);
}

.modify-modal::-webkit-scrollbar {
  width: 0 !important;
  height: 0 !important;
  display: none !important;
  background: transparent;
}

.modify-modal-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text);
  display: block;
  margin-bottom: 24rpx;
}

.modify-modal-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 24rpx;
}

.modify-modal-actions .btn {
  flex: 1;
}

.score-header {
  padding-bottom: 40rpx;
}

.score-round-label {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -1rpx;
}

.score-header-hint {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.card-label {
  font-size: 24rpx;
  color: var(--text-secondary);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1rpx;
  display: block;
  margin-bottom: 24rpx;
}

.type-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.type-chip {
  padding: 16rpx 32rpx;
  border-radius: 10rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
  background: var(--surface-solid);
  border: 1rpx solid var(--bg-muted);
  font-weight: 500;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.type-chip.chip-active {
  background: rgba(217, 119, 87, 0.12);
  color: var(--tag-primary-fg);
  border-color: var(--accent-orange);
}

.player-score-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.player-score-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx;
  background: var(--surface-solid);
  border: 1rpx solid var(--bg-muted);
  border-radius: 12rpx;
  box-shadow: 0 4rpx 16rpx rgba(20, 20, 19, 0.04);
}

.player-score-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 8rpx;
  background: var(--bg-muted);
  margin-right: 24rpx;
}

.player-score-name {
  font-size: 28rpx;
  color: var(--text);
  font-weight: 500;
}

.score-toggle {
  width: 56rpx;
  height: 56rpx;
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 600;
  margin-right: 24rpx;
}

.toggle-positive { background: rgba(120, 140, 93, 0.15); color: var(--accent-green); }
.toggle-negative { background: rgba(255, 77, 79, 0.1); color: var(--danger); }

.score-value-input {
  width: 140rpx;
  height: 80rpx;
  text-align: center;
  font-size: 36rpx;
  font-weight: 600;
  color: var(--text);
  background: var(--bg);
  border-radius: 10rpx;
  border: 1rpx solid var(--bg-muted);
}

.validation-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx;
  border-radius: 10rpx;
  margin-top: 24rpx;
}

.validation-bar.valid { background: rgba(120, 140, 93, 0.08); color: var(--accent-green); }
.validation-bar.invalid { background: rgba(255, 77, 79, 0.06); color: var(--danger); }

.remark-area {
  width: 100%;
  height: 160rpx;
  padding: 24rpx;
  background: var(--surface-solid);
  border-radius: 12rpx;
  border: 1rpx solid var(--bg-muted);
  color: var(--text);
  font-size: 28rpx;
  box-sizing: border-box;
}

.template-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  background: var(--bg);
  color: var(--text-secondary);
  border-radius: 10rpx;
  font-size: 26rpx;
  font-weight: 500;
  border: 1rpx solid var(--bg-muted);
}

.submit-area {
  padding: 32rpx;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--surface-solid);
  border-top: 1rpx solid var(--bg-muted);
  z-index: 100;
  box-shadow: 0 -8rpx 28rpx rgba(20, 20, 19, 0.06);
}

.submit-btn {
  width: 100%;
  background: linear-gradient(145deg, #e08568 0%, var(--accent-orange) 100%) !important;
  color: var(--btn-primary-text) !important;
  border-radius: 12rpx !important;
  height: 88rpx !important;
  line-height: 88rpx !important;
  font-weight: 600 !important;
}
