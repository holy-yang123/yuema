.container {
  background-color: #faf9f5;
  min-height: 100%;
  padding-bottom: 140rpx;
  box-sizing: border-box;
}

.banner {
  width: 100%;
  height: 500rpx;
  position: relative;
}

.banner image {
  width: 100%;
  height: 100%;
  filter: brightness(1);
}

.info-section {
  padding: 40rpx 32rpx;
  background-color: #faf9f5;
}

.venue-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.venue-name {
  color: #141413;
  font-size: 40rpx;
  font-weight: 600;
  letter-spacing: -1rpx;
}

.venue-rating {
  color: #e8a820;
  font-size: 28rpx;
  font-weight: 500;
}

.venue-address {
  display: flex;
  justify-content: space-between;
  color: #b0aea5;
  font-size: 26rpx;
  margin-bottom: 16rpx;
}

.venue-quick-actions {
  display: flex;
  gap: 24rpx;
  margin-bottom: 32rpx;
}

.quick-link {
  font-size: 26rpx;
  color: #d97757;
}

.distance {
  color: #788c5d;
  font-weight: 500;
}

.detail-section {
  padding: 40rpx 32rpx;
  background-color: #ffffff;
  border-top: 1rpx solid #e8e6dc;
}

.section-title {
  color: #141413;
  font-size: 32rpx;
  font-weight: 600;
  margin-bottom: 24rpx;
  position: relative;
  padding-left: 24rpx;
}

.section-title::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 4rpx;
  height: 24rpx;
  background: #d97757;
}

.section-content {
  color: #b0aea5;
  font-size: 28rpx;
  line-height: 1.8;
}

.footer-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  gap: 20rpx;
  padding: 24rpx 32rpx calc(24rpx + env(safe-area-inset-bottom));
  background: #ffffff;
  border-top: 1rpx solid #e8e6dc;
  z-index: 100;
  box-shadow: 0 -8rpx 28rpx rgba(20, 20, 19, 0.06);
}

.btn-foot {
  flex: 1;
  height: 88rpx !important;
  line-height: 88rpx !important;
  border-radius: 12rpx !important;
  font-size: 28rpx !important;
  font-weight: 600 !important;
  border: none !important;
}

.btn-foot::after {
  border: none;
}

.btn-foot.primary {
  background: linear-gradient(145deg, #e08568 0%, #d97757 100%) !important;
  color: #ffffff !important;
}

.btn-foot.secondary {
  background: #faf9f5 !important;
  color: #141413 !important;
  border: 1rpx solid #e8e6dc !important;
}
